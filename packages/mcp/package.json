{
  "name": "@codepulse/mcp",
  "version": "0.1.0",
  "private": true,
  "description": "codepulse MCP server — compact runtime + static queries for AI agents",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "codepulse-mcp": "dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "dev": "tsx src/index.ts",
    "test": "tsx src/smoke.ts"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.6.0"
  }
}
